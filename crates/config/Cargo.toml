[package]
name = "apvm-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Configuration and path management for APVM"
license = "MIT"

[dependencies]
# Serialization
serde = { workspace = true }

[dev-dependencies]
# For testing serialization
serde_json = { workspace = true }
